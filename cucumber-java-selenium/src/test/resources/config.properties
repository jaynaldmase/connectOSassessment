# Shared framework configuration — environment-level settings
# Change this to switch between environments (staging, prod, etc.)
base.url=https://the-internet.herokuapp.com
# Path to test data files on the classpath (relative to src/test/resources)
testdata.path=testdata/
